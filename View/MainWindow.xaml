<Window x:Class="evolution.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:evolution"
        mc:Ignorable="d"
        Title="Evolution" MinWidth="1060" MinHeight="600
        " Loaded="Window_Loaded">
    <Window.Resources>
            <ResourceDictionary Source="../Styles/Style.xaml" />
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Image x:Name="MainMenuBG" Source="../Resources/Images/MainMenu_img1.jpg" Stretch="UniformToFill" HorizontalAlignment="Center" VerticalAlignment="Center"/>

        <Frame x:Name="frame" Content="{Binding CurrentPage, UpdateSourceTrigger=PropertyChanged}" 
               BorderBrush="LimeGreen" BorderThickness="0"
               NavigationUIVisibility="Hidden"
               Opacity="{Binding FrameOpacity}"/>
    </Grid>
</Window>
